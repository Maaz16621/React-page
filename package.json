{
  "name": "react-page-navigation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "esbuild index.tsx --bundle --outfile=dist/app.js --jsx=automatic --loader:.tsx=tsx --servedir=dist --watch",
    "build": "esbuild index.tsx --bundle --outfile=dist/app.js --jsx=automatic --loader:.tsx=tsx --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  }
}
